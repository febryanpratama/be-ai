version: '3.8'
services:
  web:
    image: be-ai
    volumes:
      - /root/be-ai/.env:/home/node/app/.env # Path yang diperbarui sesuai direktori kerja dalam Dockerfile
    ports:
      - '8083:8083' # Sesuaikan port sesuai kebutuhan aplikasi
